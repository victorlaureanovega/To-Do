/**
 * ChartGrid Component
 * 
 * Responsive layout container for analytics charts.
 * Arranges charts in a grid that adapts to different screen sizes.
 */

export default function ChartGrid({ children }) {
	return <div className="analytics-grid">{children}</div>
}
